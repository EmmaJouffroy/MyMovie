<%= form_for(post) do |f| %>
  <% if post.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(post.errors.count, "error") %> prohibited this post from being saved:</h2>

      <ul>
      <% post.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :film %>
    <%= f.text_field :film, :required => true %>
  </div>

  <div class="field">
    <%= f.label :citation %>
    <%= f.text_area :citation, :required => true %>
  </div>

  <div class="field">
    <%= f.label :realisateur %>
    <%= f.text_field :realisateur, :required => true %>
  </div>

  <div class="field">
    <%= f.label :image %>
    <%= f.file_field :image, multiple: false %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
