<p id="notice"><%= notice %></p>
<div class="jumbotron">
<h1>Mes films</h1>
</div>
<div class="container">
  <div class="row">
    <div class="col-md-6 col-md-offset-3">
<table class="table table-striped">
  <thead class="thead">
    <tr>
      <th colspan="1"></th>
      <th>Film</th>
      <th>Citation</th>
      <th>Réalisateur</th>
    </tr>
  </thead>

  <tbody>

    <% @posts.each do |post| %>
      <tr>
        <td><% if post.image.file.nil? == false %>
        <%= image_tag post.image, width: 300%>
            <% else %>
                No Pic
                <% end %></td>

        <td><%= post.film %></td>
        <td><%= post.citation %></td>
        <td><%= post.realisateur %></td>
      </div>
    </div>
  </div>



        <td><%= link_to "Voir l'article", post %></td>
        <td><%= link_to 'Modifier', edit_post_path(post) %></td>
        <td><%= link_to 'Supprimer', post, method: :delete, data: { confirm: "Voulez-vous vraiment supprimer l'article?"  } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<button type="button" class="btn btn-"><%= link_to 'Ajouter un film', new_post_path %></button>
